<h2>
  <%= link_to @competition.name, competition_path(@competition) %>
</h2>

<h5>
  <%= @competition.gender %>&nbsp;
  <%= @competition.division %>
</h5>

<h2>Create New Round</h2>

<div>
  <%= form_for [@competition, @round] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :number, "Round Number" %>
    <%= f.number_field :number %>

    <p>(In a two-round competition, qualifiers would be "1" and finals would be "2")</p>

    <%= f.submit "Create Round", class: "button small" %>
  <% end %>
</div>
