<h2>
  <%= link_to @round.competition.name, competition_path(@round.competition) %>
</h2>

<h5>
  <%= @round.competition.gender %>&nbsp;
  <%= @round.competition.division %>
</h5>

<h2>Edit <%= link_to "#{@round.name} (Round #{@round.number})", round_path(@round) %></h2>

<div>
  <%= form_for @round do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :number, "Round Number" %>
    <%= f.number_field :number %>

    <p>(In a two-round competition, qualifiers would be "1" and finals would be "2")<p>

    <%= f.submit "Update Round", class: "button small" %>
  <% end %>
</div>
